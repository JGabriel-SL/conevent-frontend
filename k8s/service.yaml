apiVersion: v1
kind: Service
metadata:
  name: conevent-frontend-svc
spec:
  type: NodePort
  selector:
    app: conevent-frontend
  ports:
    - protocol: TCP
      port: 80 
      targetPort: 80
      nodePort: 30000
